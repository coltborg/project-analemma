import SEO from '../components/seo'

<!-- TODO: Add SEO to mdx by default and expost props via frontmatter -->
<SEO title="Uses" keywords={[`gatsby`, `application`, `react`]} />

# Uses

some paragraph

```js
import React from 'react'

const testComponent = () => (
  return (
    <h1>This is a code snippet</h1>
  )
)
```